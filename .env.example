# Optional durable storage for Vercel/serverless deployments.
# Without these, the deployed app still runs, but data is stored in warm in-memory serverless state.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Optional namespace if you share one Redis database across multiple apps.
SHORT_STUDIO_REDIS_KEY=short-studio:db
